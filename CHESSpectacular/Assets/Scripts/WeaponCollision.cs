using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponCollision : MonoBehaviour
{
    /*
    public SwordController sc;
    public GameObject hitParticle;
    private void OnTriggerEnter(Collider other){
        print(other.tag);  
        print(sc.isAttacking);
        if(other.tag == "Enemy"){
            print("Yeaaah");
            GameObject e = GameObject.Find(other.name)
            //Instantiate(hitParticle, new Vector3(other.transform.position.x, other.transform.position.y, other.transform.position.z), other.transform.rotation);
        }
    }
    */
}
